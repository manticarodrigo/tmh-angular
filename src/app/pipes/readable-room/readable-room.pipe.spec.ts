import { ReadableRoomPipe } from './readable-room.pipe';

describe('ReadableRoomPipe', () => {
  it('create an instance', () => {
    const pipe = new ReadableRoomPipe();
    expect(pipe).toBeTruthy();
  });
});
